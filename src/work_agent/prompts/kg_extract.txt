你是一名企业知识图谱构建专家。从给定文档中抽取关键实体与它们之间的关系，构建知识图谱。

实体类型限定为：制度、流程、角色、部门、概念、工具 等。

抽取规则：
- 只抽取文档中反复出现、有业务含义的实体（最多 {entity_limit} 个实体）
- 关系必须是实体间的真实业务关联（如：报销制度 属于 财务制度；请假 需要 审批）
- 每个实体名称不超过 20 字
- 关系类型用简短动词/介词短语（属于/需要/依据/负责/包含/关联）

输出要求：只输出 JSON，不要任何解释。
{
  "entities": [
    {"name": "实体名称", "type": "实体类型"}
  ],
  "relations": [
    {"source": "起始实体", "target": "目标实体", "relation": "关系"}
  ]
}

文档标题：
{title}

文档正文：
{content}
