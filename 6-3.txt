我建议你不要直接让 Claude 按“加 LangGraph”去改，因为容易变成大重构，把你现在已经稳定的 Runtime → Intent → Planner → Tool → Service 架构推翻。
你的 Work-Agent 现在其实已经有 Agent 雏形，下一步应该是：
保留现有 Agent 工具体系，引入 Graph State 作为上层编排和上下文管理层。
下面这个规划可以直接发给 Claude。
Work-Agent 升级规划：引入统一上下文管理 + LangGraph Agent Orchestration
一、升级目标
当前系统：
企微/Web
 ↓
Runtime
 ↓
Intent Router
 ↓
Planner
 ↓
Agent
 ↓
Tool
 ↓
Service
 ↓
Database
目前存在两个问题：
1. 上下文割裂
RAG：
用户问题
 ↓
知识库查询
 ↓
回答
任务：
用户命令
 ↓
任务工具
 ↓
创建任务
两者没有共享会话状态。
例如：
用户：
差旅报销标准是什么？

Agent：
根据制度回答

用户：
那按照这个制度给张三安排培训任务

当前系统不知道上一轮制度内容。
2. Agent执行状态无法恢复
例如：
管理员：
给张三安排安全培训，下周完成

流程：
解析任务
 ↓
权限检查
 ↓
等待确认
 ↓
创建任务
 ↓
发送通知
如果中途：
服务重启
网络失败
邮件发送失败
无法恢复。
二、总体架构升级
升级后：
                 用户
                  |
          Web / 企业微信
                  |
             thread_id
                  |
          LangGraph Runtime
                  |
        +---------+----------+
        |
   WorkAgentState
        |
        |
 +------+----------------+
 |                       |
chat_history        task_state
 |                       |
RAG查询              Agent执行
 |                       |
Milvus              Tools
                         |
                  DB / 邮件 / 企微
核心原则：
一个用户一个会话，一个 State。
不要：
RAG Memory
Task Memory
两套。
统一：
WorkAgentState
Phase 1：引入统一 State（基础层）
目标：
不改变业务逻辑，只增加上下文容器。
新增：
agent/state.py
定义：
class WorkAgentState(TypedDict):

    # 用户输入
    user_input:str


    # 对话历史
    chat_history:list[BaseMessage]


    # 当前用户信息
    user_id:int
    tenant_id:str
    role_codes:set[str]


    # Agent执行状态

    intent:str

    current_tool:str

    task_entity:dict | None

    rbac_result:bool | None

    db_result:dict | None

    notification_result:dict | None


    # 错误
    error:str | None
引入 thread_id
每次会话：
thread_id=session_id
来源：
Web：
conversation_id
企微：
user_id + 日期
引入 Checkpointer
生产：
PostgreSQL
新增：
langgraph.checkpoint.postgres
保存：
thread_id
        |
        |
        WorkAgentState
保存内容：
chat_history

task_entity

权限结果

工具执行状态

审计信息
验证：
新增：
test_agent_state.py
测试：
创建thread
保存state
重启恢复state
Commit:
feat: introduce unified agent state and checkpoint
Phase 2：RAG接入统一上下文
目标：
RAG 使用 chat_history。
不是简单问答。
新增节点：
rag_node.py
流程：
用户问题

↓

读取chat_history

↓

query rewrite

↓

Milvus检索

↓

LLM回答

↓

追加chat_history

↓

checkpoint
例如：
历史：
用户：
差旅标准是什么？

AI：
住宿标准如下...
当前：
用户：
那经理呢？
query rewrite：
生成：
公司差旅经理住宿标准
再查Milvus。
注意：
禁止：
chat_history embedding
禁止：
聊天记录进入知识库
新增：
history_manager.py
负责：
滑动窗口
默认：
最近6轮。
例如：
保留：
user
assistant
user
assistant
user
assistant
超过：
删除旧消息。
验证：
测试：
test_rag_memory.py
场景：

问制度

追问：
那经理呢？


验证仍能理解。
Commit:
feat: add conversational rag memory
Phase 3：Agent执行Graph化
目标：
任务执行进入 LangGraph。
不是聊天机器人。
Graph：
START

 |

trim_history

 |

intent_router

 |

 +----------------+
 |                |
RAG             TASK
 |                |
answer          planner

                 |

            permission_check

                 |

             tool_execute

                 |

          confirmation?

          /       \

       yes        no

       |

     database

       |

   notification

       |

      END
新增节点：
1. intent_node
负责：
判断：
knowledge_query

task_create

task_query

task_remind
2. planner_node
输出：
例如：
{
"tool":"task_tool",
"action":"create",
"args":{
"user":"张三",
"title":"安全培训"
}
}
3. permission_node
所有危险动作必须经过：
RBAC
+
department scope
例如：
部门经理：
可以：
研发部员工
不能：
财务部员工
4. tool_node
调用：
现有：
task_tool

knowledge_tool

notification_tool
不重写。
验证：
测试：
test_graph_execution.py
场景：
管理员：
给张三安排培训任务
检查：
state：
task_entity

db_result

notification_result
Commit:
feat: migrate agent execution to langgraph workflow
Phase 4：任务创建人工确认流程
目标：
真正企业Agent。
流程：
管理员：
给张三安排安全培训，下周五完成

Graph：
intent

↓

task_create

↓

extract_entity

↓

RBAC

↓

preview

↓

interrupt
返回：
确认任务：

执行人:
张三

任务:
安全培训

截止:
2026-08-21


回复：
确认 / 取消
用户：
确认
恢复：
checkpoint(thread_id)

↓

create_task

↓

send_wechat

↓

END
新增：
表：
task_pending_create
字段：
id

thread_id

creator_id

employee_id

task_data

status

created_at
Commit:
feat: add human confirmation workflow
Phase 5：主动督办Agent化
目标：
从定时脚本升级为Agent。
当前：
scheduler
 |
查询任务
 |
发送提醒
升级：
scheduler

↓

Reminder Agent

↓

分析任务风险

↓

生成行动计划

↓

调用notification_tool

State:
增加：
risk_tasks

reminder_plan

例如：
发现：
张三：
任务延期3天
Agent：
决定：
第一次:
企微提醒

第二次:
邮件提醒

第三次:
通知主管
Commit:
feat: add reminder agent workflow
Phase 6：生产级Memory管理
会话存储
PostgreSQL：
LangGraph checkpoint。
thread:
tenant_id:user_id:session
chat_history策略
短：
6轮窗口。
长：
摘要。
新增：
conversation_summary
结构：
summary

recent_messages
审计
所有Agent行为记录：
agent_execution_log

thread_id

user

intent

tool

action

result

confirmed

created_at
满足企业审计。
最终架构
                 企业微信
                     |
                    Web
                     |
                 thread_id
                     |
              LangGraph Runtime
                     |
              WorkAgentState
                     |
      +--------------+--------------+
      |                             |
 chat_history                  task_state
      |                             |
 Query Rewrite                 Planner
      |                             |
 RAG Node                     RBAC Node
      |                             |
 Milvus                        Tool Node
                                    |
                         DB / Email / Wechat
实施原则
不推翻现有 Tool/Service 层

LangGraph 只负责：
状态
流程
恢复
多步骤编排

RAG 和 Agent 共用：
thread_id
WorkAgentState
chat_history
所有企业动作必须经过：
Graph Edge控制
+
RBAC
+
Confirmation
分 Phase：
每阶段：
修改
测试
commit
部署验证
不要一次改几十个文件。
这个版本比 Claude 原来的“增加几个 tool”更接近真正企业 Agent，因为它补上了 Agent 最核心的三件事：
State（记忆）
Workflow（流程图）
Tool Orchestration（工具编排）
否则只是“带工具的 RAG 聊天机器人”。你现在这个升级方向才会明显拉开和普通 RAG 项目的差距。